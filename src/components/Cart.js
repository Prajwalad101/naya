import React from 'react';

// import { data } from '../data';

export const Cart = ({ idList }) => {
  return (
    <div>
      {idList.forEach((id) => {
        <h1>id</h1>;
      })}
    </div>
  );
};
